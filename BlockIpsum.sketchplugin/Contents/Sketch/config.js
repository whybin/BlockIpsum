const Config = {    // eslint-disable-line no-unused-vars
    defaults: {
        fontSize: 16,
        groupName: 'Text Blocks'
    },

    // Relative to font size
    ratios: {
        capHeight: 0.7,
        lineSpacing: 1.5,
        precision: 0.5
    }
};
